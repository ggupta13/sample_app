<% provide(:title, 'Add Member') %>
<h1>Please select a member that you would like to add:</h1>
<%= @team.team_name %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, add_to_team_path(:team_id => @team, :user_id => user) %>
    </li>
  <% end %>
</ul>